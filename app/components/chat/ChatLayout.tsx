import { cookies } from "next/headers";
import React from "react";
interface ChatLayoutProps {
  defaultLayout: string;
}

const ChatLayout = () => {
  return <div data-theme={""}>ChatLayout</div>;
};

export default ChatLayout;
