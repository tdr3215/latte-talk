"use client";
import React from "react";

const ButtonComp = () => {
  return (
    <button
      onClick={() => {
        console.log("click");
      }}
    ></button>
  );
};

export default ButtonComp;
